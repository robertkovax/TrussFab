{
  "name": "TrussFabUI",
  "description": "parent package for in-sketchup web-based UI panels of TrussFab (residing in /ui)",
  "scripts": {
    "postinstall": "(cd ui/animation-pane && npm ci && npm build) && (cd ui/sidebar && npm ci) && (cd ui/force-chart && npm ci)"
  }
}
