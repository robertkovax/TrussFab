const xAxis = 300;
const yAxis = 50;
const timelineStepSeconds = 100; // this affects how smooth the line should be

const FACTOR = 0.5;

const DEV = false;

export { xAxis, yAxis, timelineStepSeconds, FACTOR, DEV };
